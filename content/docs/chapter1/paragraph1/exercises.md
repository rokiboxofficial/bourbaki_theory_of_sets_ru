---
math: true
title: "Упражнения"
weight: 10
---

# Упражнения

## Упражнение 1)

### 1.1

Пусть $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ — формативная конструкция теории $\mathcal{T}$, где $\boldsymbol{A}_n$ — единственное знакосочетание **второго рода**.

По определению формативной конструкции, для каждого знакосочетания $\boldsymbol{A}$ из последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ должно выполняться одно из условий. Покажем, что для $\boldsymbol{A}_n$ не выполняется ни одно из них:

а) $\boldsymbol{A}_n$ является знакосочетанием второго рода, значит, по определению знакосочетания второго рода, $\boldsymbol{A}_n$ не сводится к одной букве. $\boldsymbol{A}_n$ — не буква.

б, в, г) $\boldsymbol{A}_n$ — единственное знакосочетание второго рода в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, значит, никакое другое знакосочетание второго рода не предшествует $\boldsymbol{A}_n$ в $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$.

д) теория $\mathcal{T}$ не содержит специальных знаков.

Такой формативной конструкции теории $\mathcal{T}$ не существует. Теория $\mathcal{T}$ не содержит соотношений.

### 1.2

Пусть $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ — формативная конструкция теории $\mathcal{T}$, где $\boldsymbol{A}_n$ — единственное знакосочетание **первого рода**, не сводящееся к одной букве.

По определению формативной конструкции, для каждого знакосочетания $\boldsymbol{A}$ из последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ должно выполняться одно из условий. Покажем, что для $\boldsymbol{A}_n$ не выполняется ни одно из них:

а) $\boldsymbol{A}_n$ — знакосочетание первого рода, не сводящееся к одной букве. $\boldsymbol{A}_n$ — не буква.

б, в, г) согласно 1.1, теория $\mathcal{T}$ не содержит соотношений, значит, ни одно знакосочетание второго рода не предшествует $\boldsymbol{A}_n$ в $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$.

д) теория $\mathcal{T}$ не содержит специальных знаков.

Такой формативной конструкции теории $\mathcal{T}$ не существует. Единственные знакосочетания теории $\mathcal{T}$, являющиеся термами, суть знакосочетания, сводящиеся к одной букве.

### 1.3

Из 1.1 и 1.2 следует, что теория $\mathcal{T}$ не содержит соотношений, а единственные знакосочетания теории $\mathcal{T}$, являющиеся термами, суть знакосочетания, сводящиеся к одной букве.


## Упражнение 2)

### 2.1

Пусть $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ - формативная конструкция теории $\mathcal{T}$, где $\boldsymbol{A}_n$ единственное знакосочетание, которое содержит знак $\square$, не связанный с одним и только одним знаком $\tau$ слева от него. По определению формативной конструкции, для каждого знакосочетания $\boldsymbol{A}$ из $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ должно выполняться одно из условий. Покажем, что для $\boldsymbol{A}_n$ не выполняется ни одно из условий:

а) $\boldsymbol{A}_n$ содержит знак $\square$, значит, $\boldsymbol{A}_n$ не буква.

б, в, д) $\boldsymbol{A}_n$ это знакосочетание, полученное написанием слева направо знака веса n, отличного от $\square$, и n знакосочетаний из предществующих $\boldsymbol{A}_n$ знакосочетаний. Так как в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием, которое содержит знак $\square$, не связанный с одним и только одним знаком $\tau$ слева от него, то в предществующих $\boldsymbol{A}_n$ знакосочетаниях, такого знака нет, значит, знакосочетание, полученное написанием слева направо знака веса n, отличного от $\square$, и n знакосочетаний из предществующих $\boldsymbol{A}_n$ знакосочетаний, также его не содержит.

г) $\boldsymbol{A}_n$ есть $\tau_x(\boldsymbol{B})$, где $\boldsymbol{B}$ - знакосочетание второго рода, предшествующее $\boldsymbol{A}_n$. Так как в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием, которое содержит знак $\square$, не связанный с одним и только одним знаком $\tau$ слева от него, то $\boldsymbol{B}$ не содержит такого знака. Знакосочетание $\tau_x(\boldsymbol{B})$, по определению, не содержит связей, отличных от связей $\square$ со знаком $\tau$, который находится слева от $\square$, и для каждого $\square$ такая связь одна и только одна.

Такой формативной конструкции теории $\mathcal{T}$ не существует. Пусть $\boldsymbol{A}$ - терм или соотношение теории $\mathcal{T}$. Каждый знак $\square$, если он имеется в $\boldsymbol{A}$, связан с одним и только одним знаком $\tau$, расположенным слева от него

### 2.2

Пусть $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ - формативная конструкция теории $\mathcal{T}$, где $\boldsymbol{A}_n$ единственное знакосочетание, которое содержит знак $\tau$, имеющий хотя бы одну связь с каким-либо знаком, но отличным от знака $\square$, который находится справа него. По определению формативной конструкции, для каждого знакосочетания $\boldsymbol{A}$ из $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ должно выполняться одно из условий. Покажем, что для $\boldsymbol{A}_n$ не выполняется ни одно из условий:

а) $\boldsymbol{A}_n$ содержит знак $\tau$, значит, $\boldsymbol{A}_n$ не буква.

б, в, д) $\boldsymbol{A}_n$ это знакосочетание, полученное написанием слева направо знака веса n, и n знакосочетаний из предществующих $\boldsymbol{A}_n$ знакосочетаний. Так как в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием, которое содержит знак $\tau$, имеющий хотя бы одну связь с каким-либо знаком, но отличным от знака $\square$, который находится справа от него, то в предществующих $\boldsymbol{A}_n$ знакосочетаниях, такого знака $\tau$ нет, значит, знакосочетание, полученное написанием слева направо знака веса n, и n знакосочетаний из предществующих $\boldsymbol{A}_n$ знакосочетаний, также его не содержит.

г) $\boldsymbol{A}_n$ есть $\tau_x(\boldsymbol{B})$, где $\boldsymbol{B}$ - знакосочетание второго рода, предшествующее $\boldsymbol{A}_n$. Так как в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием, которое содержит знак $\tau$, имеющий хотя бы одну связь с любым знаком (отличным от знака $\square$, который находится справа от него), то $\boldsymbol{B}$ не содержит такого знака. Знакосочетание $\tau_x(\boldsymbol{B})$, по определению, не содержит связей, отличных от связей $\tau$ со знаками $\square$, которые находятся справа от данного знака $\tau$.

Такой формативной конструкции теории $\mathcal{T}$ не существует.

Пусть $\boldsymbol{A}$ - терм или соотношение теории $\mathcal{T}$. Всякий знак $\tau$, если он имеется в $\boldsymbol{A}$, либо не связан с другими знаками, либо связан с некоторыми знаками $\square$, расположенными справа от него.

### 2.3

Пусть $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ - формативная конструкция теории $\mathcal{T}$, где $\boldsymbol{A}_n$ единственное знакосочетание, которое содержит связи, отличные от связей знака $\square$ со знаком $\tau$. По определению формативной конструкции, для каждого знакосочетания $\boldsymbol{A}$ из $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ должно выполняться одно из условий. Покажем, что для $\boldsymbol{A}_n$ не выполняется ни одно из условий:

а) $\boldsymbol{A}_n$ содержит связи, значит, $\boldsymbol{A}_n$ не буква.

б, в, д) $\boldsymbol{A}_n$ это знакосочетание, полученное написанием слева направо знака веса n, и n знакосочетаний из предществующих $\boldsymbol{A}_n$ знакосочетаний. Так как в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием, которое содержит связи, отличные от связей знака $\square$ со знаком $\tau$, то в предществующих $\boldsymbol{A}_n$ знакосочетаниях, таких связей нет, значит, знакосочетание, полученное написанием слева направо знака веса n, и n знакосочетаний из предществующих $\boldsymbol{A}_n$ знакосочетаний, не содержит свзяей, отличных от связей знака $\square$ со знаком $\tau$ 

г) $\boldsymbol{A}_n$ есть $\tau_x(\boldsymbol{B})$, где $\boldsymbol{B}$ - знакосочетание второго рода, предшествующее $\boldsymbol{A}_n$. Так как в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием, которое содержит связи знака $\square$ со знаком $\tau$, то $\boldsymbol{B}$ не содержит таких связей. Знакосочетание $\tau_x(\boldsymbol{B})$, по определению, не содержит связей, отличных от связей $\tau$ со знаками $\square$. 

Такой формативной конструкции теории $\mathcal{T}$ не существует.

Пусть $\boldsymbol{A}$ - терм или соотношение теории $\mathcal{T}$. Из 2.1 и 2.2 следует, что каждый знак $\square$, если он имеется в $\boldsymbol{A}$, связан с одним и только одним знаком $\tau$, расположенным слева от него. Всякий знак $\tau$, если он имеется в $\boldsymbol{A}$, либо не связан с другими знаками, либо связан с некоторыми знаками $\square$, расположенных справа от него. Ни один другой знак не связан ни с какими знаками.

## Упражнение 3)

Пусть $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ - формативная конструкция теории $\mathcal{T}$, где $\boldsymbol{A}_n$ единственное знакосочетание, которое содержит специальный знак, который не предшествует знаку $\square$, или знаку $\tau$, или букве, или субстантивному знаку. По определению формативной конструкции, для каждого знакосочетания $\boldsymbol{A}$ из $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$ должно выполняться одно из условий. Покажем, что для $\boldsymbol{A}_n$ не выполняется ни одно из условий:


а) $\boldsymbol{A}_n$ содержит специальный знак, значит, $\boldsymbol{A}_n$ не буква.

б, в) $\boldsymbol{A}_n$ это знакосочетание, полученное написанием слева направо знака $\lnot$ (соответственно $\lor$), и одного знакосочетания (соотвественно двух знакосочетаний) из предществующих $\boldsymbol{A}_n$ знакосочетаний. Так как в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием, которое содержит специальный знак, который не предшествует знаку $\square$, или знаку $\tau$, или букве, или субстантивному знаку, то в предществующих $\boldsymbol{A}_n$ знакосочетаниях, такого знака нет. Знакосочетание, полученное написанием слева направо знака $\lnot$ (соответственно $\lor$), и одного знакосочетания (соответственно двух знакосочетаний) из предществующих $\boldsymbol{A}_n$ знакосочетаний, не содержит такого специального знака.

г) $\boldsymbol{A}_n$ есть $\tau_x(\boldsymbol{B})$, где $\boldsymbol{B}$ - знакосочетание второго рода, предшествующее $\boldsymbol{A}_n$. Рассмотрим операцию перехода от $\boldsymbol{B}$ к $\tau_x(\boldsymbol{B})$. Мы должны поставить знак $\tau$ перед $\boldsymbol{B}$ и соединить связью каждый экземпляр буквы $\boldsymbol{x}$ в $\boldsymbol{B}$ со знаком $\tau$, написанным слева от $\boldsymbol{B}$, и заменить букву $\boldsymbol{x}$, каждый ее экземпляр, символом $\square$. Эта операция не приводит к появлению специального знака в $\tau_x(\boldsymbol{B})$, который не предшествует знаку $\square$, или знаку $\tau$, или букве, или субстантивному знаку, ведь $\boldsymbol{B}$ не содержит такого специального знака, так как знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, которое содержит такой специальный знак. Если знак, следующий за специальным знаком в $\boldsymbol{B}$ - буква $\boldsymbol{x}$, он заменится на $\square$, что также допустимо.

д) $\boldsymbol{A}_n$ это знакосочетание, полученное написанием слева направо специального знака веса n, и n знакосочетаний первого рода из предществующих $\boldsymbol{A}_n$ знакосочетаний. Рассмотрим операцию перехода от n знакосочетаний первого рода, предществующих $\boldsymbol{A}_n,$ к $\boldsymbol{A}_n$. Мы должны написать слева направо специальный знак веса n, и n знакосочетаний первого рода из предществующих $\boldsymbol{A}_n$ знакосочетаний. Специальный знак веса n, написанный слева, предшествует знакосочетанию первого рода, которое по определению, начинается со знака $\tau$, или с субстантивного знака, или с буквы. В написанных справа от специального знака знакосочетания первого рода, предшествующих $\boldsymbol{A}_n$, каждый специальный знак, если он имеется в них, передшествует знаку $\square$, или знаку $\tau$, или букве, или субстантивному знаку, так как знакосочетание $\boldsymbol{A}_n$ является единственным знакосочетанием в последовательности $\boldsymbol{A}_1, \boldsymbol{A}_2, \dots, \boldsymbol{A}_n$, которое содержит специальный знак, который не предшествует знаку $\square$, или знаку $\tau$, или букве, или субстантивному знаку. Значит, $\boldsymbol{A}_n$ не содержит специального знака, который не предшествует знаку $\square$, или знаку $\tau$, или букве, или субстантивному знаку.

Такой формативной конструкции теории $\mathcal{T}$ не существует.

Пусть $\boldsymbol{A}$ - терм или соотношение теории $\mathcal{T}$. Каждый специальный знак, если он имеется в $\boldsymbol{A}$, предшествует некоторому знаку $\square$, или знаку $\tau$, или букве, или субстантивному знаку.

## Упражнение 4)

### 4.1

Пусть $\boldsymbol{A}$, $\boldsymbol{B}$, $\boldsymbol{C}$, $\boldsymbol{D}$, $\boldsymbol{W}$, $\boldsymbol{E}$ суть знакосочетания теории $\mathcal{T}$, причем $\boldsymbol{AB}$ есть $\boldsymbol{CD}$. Пусть длина $\boldsymbol{A}$ меньше длины $\boldsymbol{C}$, покажем, что $\boldsymbol{AW}$ есть $\boldsymbol{C}$, а $\boldsymbol{ED}$ есть $\boldsymbol{B}$.

Пусть k есть длина $\boldsymbol{A}$, а l есть длина $\boldsymbol{C}$. Так как $\boldsymbol{AB}$ есть $\boldsymbol{CD}$, первые k знаков $\boldsymbol{AB}$ суть первые k знаков $\boldsymbol{CD}$, но длина $\boldsymbol{C}$ больше k, а длина $\boldsymbol{A}$ есть k. Значит, знакосочетание, полученное записью слева направо знаков под номерами 1, ..., k из $\boldsymbol{C}$ и связей между ними, есть $\boldsymbol{A}$. Пусть $\boldsymbol{W}$ есть знакосочетание, полученное записью слева направо знаков под номерами k + 1, ..., l из $\boldsymbol{C}$ и связей между ними. Пусть $\boldsymbol{AW}$ не есть $\boldsymbol{C}$, значит, либо последовательность знаков $\boldsymbol{AW}$ не есть последовательность знаков $\boldsymbol{C}$, либо связи $\boldsymbol{AW}$ не суть связи $\boldsymbol{C}$. $\boldsymbol{AW}$ есть знакосочетание, полученное записью слева направо знаков под номерами 1, ..., k из $\boldsymbol{C}$ и связей между ними, и знаков под номерами k + 1, ..., l из $\boldsymbol{C}$ и связей между ними. Последовательность знаков $\boldsymbol{AW}$ есть последовательность знаков $\boldsymbol{C}$. Пусть существует связь между знаками под номерами a и b из $\boldsymbol{C}$, которой нету в $\boldsymbol{AW}$. Если каждый из a и b <= k, то они есть в $\boldsymbol{A}$. Если каждый из a и b > k, то они есть в $\boldsymbol{W}$, по определению $\boldsymbol{W}$. Если a <= k, а b > k, то $\boldsymbol{AB}$ не есть $\boldsymbol{CD}$, ведь существует связь в $\boldsymbol{CD}$, которой нету в $\boldsymbol{AB}$, так как одна из связанных букв не имеет связи в $\boldsymbol{A}$. Связей со знаками вне знакосочетания нет ни в каком знакосочетании, по определению знакосочетания. Каждая связь в $\boldsymbol{AW}$ есть связь $\boldsymbol{C}$, так как мы показали это для $\boldsymbol{A}$, и по определению $\boldsymbol{W}$, а значит в $\boldsymbol{AW}$ нет связей, которых нет в $\boldsymbol{C}$. Знакосочетание $\boldsymbol{AW}$ есть $\boldsymbol{C}$.

Пусть k есть длина $\boldsymbol{D}$, а l есть длина $\boldsymbol{В}$. Так как $\boldsymbol{AB}$ есть $\boldsymbol{CD}$, последние k знаков $\boldsymbol{AB}$ суть последние k знаков $\boldsymbol{CD}$, но длина $\boldsymbol{B}$ больше k, а длина $\boldsymbol{D}$ есть k. Значит, знакосочетание, полученное записью слева направо знаков под номерами l - (k - 1), l - (k - 2), ..., l - (k - k) из $\boldsymbol{B}$ и связей между ними, есть $\boldsymbol{D}$. Пусть $\boldsymbol{E}$ есть знакосочетание, полученное записью слева направо знаков под номерами 1, ..., l - k из $\boldsymbol{B}$ и связей между ними. Пусть $\boldsymbol{ED}$ не есть $\boldsymbol{B}$, значит, либо последовательность знаков $\boldsymbol{ED}$ не есть последовательность знаков $\boldsymbol{B}$, либо связи $\boldsymbol{ED}$ не суть связи $\boldsymbol{B}$. $\boldsymbol{ED}$ есть знакосочетание, полученное записью слева направо знаков под номерами 1, ..., l - k из $\boldsymbol{B}$ и связей между ними, и знаков под номерами l - (k - 1), ..., l - (k - k) из $\boldsymbol{B}$ и связей между ними. Последовательность знаков $\boldsymbol{ED}$ есть последовательность знаков $\boldsymbol{B}$. Пусть существует связь между знаками под номерами a и b из $\boldsymbol{B}$, которой нету в $\boldsymbol{ED}$. Если каждый из a и b <= l - k, то они есть в $\boldsymbol{E}$, по определению $\boldsymbol{E}$. Если каждый из a и b > l - k, то они есть в $\boldsymbol{D}$. Если a <= l - k, а b > l - k, то $\boldsymbol{AB}$ не есть $\boldsymbol{CD}$, ведь существует связь в $\boldsymbol{AB}$, которой нету в $\boldsymbol{CD}$, так как одна из связанных букв не имеет связи в $\boldsymbol{D}$. Связей со знаками вне знакосочетания нет ни в каком знакосочетании, по определению знакосочетания. Каждая связь в $\boldsymbol{ED}$ есть связь $\boldsymbol{B}$, так как мы показали это для $\boldsymbol{D}$, и по определению $\boldsymbol{E}$, а значит в $\boldsymbol{ED}$ нет связей, которых нет в $\boldsymbol{D}$. Знакосочетание $\boldsymbol{ED}$ есть $\boldsymbol{B}$.

### 4.2

Пусть $\boldsymbol{H}$, $\boldsymbol{J}$, $\boldsymbol{K}$, $\boldsymbol{L}$, $\boldsymbol{G}$ суть знакосочетания теории $\mathcal{T}$, причем $\boldsymbol{HJ}$ есть $\boldsymbol{KL}$. Пусть длина $\boldsymbol{H}$ меньше длины $\boldsymbol{K}$, покажем, что $\boldsymbol{HG}$ есть $\boldsymbol{K}$, а $\boldsymbol{GL}$ есть $\boldsymbol{J}$.

Согласно 4.1, $\boldsymbol{W}$ и $\boldsymbol{E}$ суть знакосочетания теории $\mathcal{T}$ такие, что $\boldsymbol{HW}$ есть $\boldsymbol{K}$, а $\boldsymbol{EL}$ есть $\boldsymbol{J}$. Значит, $\boldsymbol{HEL}$ есть $\boldsymbol{HWL}$, то есть $\boldsymbol{E}$ есть $\boldsymbol{W}$. Пусть $\boldsymbol{G}$ есть знакосочетание $\boldsymbol{E}$, значит, $\boldsymbol{K}$ есть $\boldsymbol{HG}$, а $\boldsymbol{GL}$ есть $\boldsymbol{J}$.

### 4.3

Пусть $\boldsymbol{A}$, $\boldsymbol{B}$, $\boldsymbol{C}$, $\boldsymbol{D}$, $\boldsymbol{W}$ суть знакосочетания теории $\mathcal{T}$, причем $\boldsymbol{AB}$ есть $\boldsymbol{CD}$. Если длина $\boldsymbol{A}$ меньше длины $\boldsymbol{C}$, то $\boldsymbol{AW}$ есть $\boldsymbol{C}$, а $\boldsymbol{WD}$ есть $\boldsymbol{B}$. Если длина $\boldsymbol{A}$ больше длины $\boldsymbol{C}$, то $\boldsymbol{CW}$ есть $\boldsymbol{A}$, а $\boldsymbol{WB}$ есть $\boldsymbol{D}$. 

Пусть длина $\boldsymbol{A}$ меньше длины $\boldsymbol{C}$, согласно 4.2, $\boldsymbol{AW}$ есть $\boldsymbol{C}$, а $\boldsymbol{WD}$ есть $\boldsymbol{B}$.

Пусть длина $\boldsymbol{A}$ больше длины $\boldsymbol{C}$, согласно 4.2 $\boldsymbol{CW}$ есть $\boldsymbol{A}$, а $\boldsymbol{WB}$ есть $\boldsymbol{D}$.  


### 4.4

Пусть $\boldsymbol{R}$ есть соотношение или терм теории $\mathcal{T}$.

Если первый знак $\boldsymbol{R}$ есть $\lnot$, то $\boldsymbol{R}$, по определению соотношения, есть соотношение, которое встречается в формативных конструкциях теории $\mathcal{T}$ и для него выполняется одно из условий формативной конструкции. Из всех условий, для знакосочетания $\boldsymbol{R}$ выполняется лишь б), так как первый знак $\boldsymbol{R}$ есть $\lnot$. То есть $\boldsymbol{R}$ есть $\lnot\boldsymbol{R}_j$, где $\boldsymbol{R}_j$ есть соотношение теории $\mathcal{T}$.

Если первый знак $\boldsymbol{R}$ есть $\lor$, то $\boldsymbol{R}$, по определению соотношения, есть соотношение, которое встречается в формативных конструкциях теории $\mathcal{T}$ и для него выполняется одно из условий формативной конструкции. Из всех условий, для знакосочетания $\boldsymbol{R}$ выполняется лишь в), так как первый знак $\boldsymbol{R}$ есть $\lor$. То есть $\boldsymbol{R}$ есть $\lor\boldsymbol{R}_j\boldsymbol{R}_k$, где $\boldsymbol{R}_j$, $\boldsymbol{R}_k$ суть соотношения теории $\mathcal{T}$.

Если первый знак $\boldsymbol{R}$ есть $\tau$, то $\boldsymbol{R}$, по определению терма, есть терм, который встречается в формативных конструкциях теории $\mathcal{T}$ и для него выполняется одно из условий формативной конструкции. Из всех условий, для знакосочетания $\boldsymbol{R}$ выполняется лишь г), так как первый знак $\boldsymbol{R}$ есть $\tau$. То есть $\boldsymbol{R}$ есть $\tau_x(\boldsymbol{R}_j)$, где $\boldsymbol{R}_j$ есть соотношение теории $\mathcal{T}$, а $\boldsymbol{x}$ - буква.

Если первый знак $\boldsymbol{R}$ есть $\boldsymbol{s}$, где $\boldsymbol{s}$ специальный знак веса n из $\mathcal{T}$, то $\boldsymbol{R}$, по определению терма и соотношения, есть терм или соотношение, которое встречается в формативных конструкциях теории $\mathcal{T}$ и для него выполняется одно из условий формативной конструкции. Из всех условий, для знакосочетания $\boldsymbol{R}$ выполняется лишь д), так как первый знак $\boldsymbol{R}$ есть $\boldsymbol{s}$. То есть $\boldsymbol{R}$ имеет вид $\boldsymbol{s}\boldsymbol{R}_1\boldsymbol{R}_2...\boldsymbol{R}_n$, где $\boldsymbol{R}_1$, $\boldsymbol{R}_2$, ..., $\boldsymbol{R}_n$ суть n термов теории $\mathcal{T}$.

### 4.5

Пусть $\boldsymbol{A}$ - соотношение или терм теории $\mathcal{T}$, $\boldsymbol{A}_n$ - соотношение или терм этой теории длины n, а $\boldsymbol{B}$ - знакосочетание этой теории.

Покажем шаг за шагом, $\boldsymbol{AB}$ не есть $\boldsymbol{A}_n$. Предположим, что мы установили утверждение для всех термов и соотношений $\boldsymbol{A}_t$ длины $\boldsymbol{t}$, где $\boldsymbol{t}$ меньше $\boldsymbol{n}$, и установим его для $\boldsymbol{A}_n$.

Предположим, что $\boldsymbol{AB}$ есть $\boldsymbol{A}_n$. Для каждого терма или соотношения, по определению, должно выполняться одно из условий формативной конструкции. Покажем, что для $\boldsymbol{A}_n$ не выполняется ни одно из условий:

а) $\boldsymbol{A}_n$ есть буква. Знакосочетание $\boldsymbol{AB}$ имеет длину, как минимум, 2. Значит, $\boldsymbol{AB}$ не есть $\boldsymbol{A}_n$

б) $\boldsymbol{A}_n$ есть $\lnot\boldsymbol{A}_j$, где $\boldsymbol{A}_j$ есть соотношение теории $\mathcal{T}$. Так как $\boldsymbol{AB}$ есть $\boldsymbol{A}_n$, значит, $\boldsymbol{A}$ начинается со знака $\lnot$. Согласно 4.4, $\boldsymbol{A}$ есть $\lnot\boldsymbol{A}_m$, где $\boldsymbol{A}_m$ есть соотношение теории $\mathcal{T}$. Значит, $\lnot\boldsymbol{A}_j$ есть $\lnot\boldsymbol{A}_m\boldsymbol{B}$, то есть $\boldsymbol{A}_j$ есть $\boldsymbol{A}_m\boldsymbol{B}$. Длина знакосочетания $\boldsymbol{A}_m\boldsymbol{B}$ меньше длины $\boldsymbol{A}_n$, но мы уже установили, что термы и соотношения длины меньше чем n, не есть $\boldsymbol{HY}$, где $\boldsymbol{H}$ - терм или соотношение теории $\mathcal{T}$, а $\boldsymbol{Y}$ - знакосочетание теории $\mathcal{T}$.

в) $\boldsymbol{A}_n$ есть $\lor\boldsymbol{A}_j\boldsymbol{A}_k$, где $\boldsymbol{A}_j$ и $\boldsymbol{A}_k$ суть соотношения теории $\mathcal{T}$. Так как $\boldsymbol{AB}$ есть $\boldsymbol{A}_n$, значит, $\boldsymbol{A}$ начинается со знака $\lor$. Согласно 4.4, $\boldsymbol{A}$ есть $\lor\boldsymbol{A}_m\boldsymbol{A}_q$, где $\boldsymbol{A}_m$ и $\boldsymbol{A}_q$ суть соотношения теории $\mathcal{T}$. Значит, $\lor\boldsymbol{A}_j\boldsymbol{A}_k$ есть $\lor\boldsymbol{A}_m\boldsymbol{A}_q\boldsymbol{B}$.

Если длина соотношения $\boldsymbol{A}_j$ есть длина соотношения $\boldsymbol{A}_m$, значит, $\boldsymbol{A}_k$ есть $\boldsymbol{A}_q\boldsymbol{B}$. Длина знакосочетания $\boldsymbol{A}_q\boldsymbol{B}$ меньше длины $\boldsymbol{A}_n$, но мы уже установили, что термы и соотношения длины меньше чем n, не есть $\boldsymbol{HY}$, где $\boldsymbol{H}$ - терм или соотношение теории $\mathcal{T}$, а $\boldsymbol{Y}$ - знакосочетание теории $\mathcal{T}$.

Пусть $\boldsymbol{Y}$ - знакосочетание теории $\mathcal{T}$. Если длина соотношения $\boldsymbol{A}_j$ не есть длина соотношения $\boldsymbol{A}_m$, значит, согласно 4.3, либо $\boldsymbol{A}_j$ есть $\boldsymbol{A}_m\boldsymbol{Y}$, либо $\boldsymbol{A}_m$ есть $\boldsymbol{A}_j\boldsymbol{Y}$. Длина каждого из знакосочетаний $\boldsymbol{A}_m\boldsymbol{Y}$ и $\boldsymbol{A}_j\boldsymbol{Y}$ меньше длины $\boldsymbol{A}_n$, но мы уже установили, что термы и соотношения длины меньше чем n, не есть $\boldsymbol{HY}$, где $\boldsymbol{H}$ - терм или соотношение теории $\mathcal{T}$, а $\boldsymbol{Y}$ - знакосочетание теории $\mathcal{T}$.

г) $\boldsymbol{A}_n$ есть $\tau_x(\boldsymbol{A}_j)$, где $\boldsymbol{A}_j$ есть соотношение теории $\mathcal{T}$. Так как $\boldsymbol{AB}$ есть $\boldsymbol{A}_n$, значит, $\boldsymbol{A}$ начинается со знака $\tau$. Согласно 4.4, $\boldsymbol{A}$ есть $\tau_y(\boldsymbol{A}_m)$, где $\boldsymbol{A}_m$ есть соотношение теории $\mathcal{T}$. Значит, $\tau_x(\boldsymbol{A}_j)$ есть $\tau_y(\boldsymbol{A}_m)\boldsymbol{B}$.

1. Пусть $\boldsymbol{I}$, $\boldsymbol{O}$ суть соотношения теории $\mathcal{T}$, $\boldsymbol{w}$  - буква, а соотношение $\tau_w(\boldsymbol{I})$ этой теории есть соотношение $\tau_w(\boldsymbol{O})$ этой теории. Покажем, что $\boldsymbol{I}$ есть $\boldsymbol{O}$. Пусть $\boldsymbol{I}$ не есть $\boldsymbol{O}$.

    Так как $\boldsymbol{I}$ не есть $\boldsymbol{O}$, то либо длина $\boldsymbol{I}$ не есть длина $\boldsymbol{O}$, либо последовательность знаков $\boldsymbol{I}$ не есть последовательность знаков $\boldsymbol{O}$, либо связи $\boldsymbol{I}$ не суть связи $\boldsymbol{O}$.

    Пусть длина знакосочетания $\boldsymbol{I}$ не есть длина знакосочетания $\boldsymbol{O}$. По определению операции $\tau_u(\boldsymbol{C})$, где $\boldsymbol{C}$ - знакосочетание, а $\boldsymbol{u}$ - буква, знакосочетание $\tau_u(\boldsymbol{C})$ имеет длину на 1 больше, чем знакосочетание $\boldsymbol{C}$. Значит, знакосочетание $\tau_w(\boldsymbol{I})$ имеет длину на 1 больше, чем знакосочетание $\boldsymbol{I}$, а знакосочетание $\tau_w(\boldsymbol{O})$ имеет длину на 1 больше, чем знакосочетание $\boldsymbol{O}$. Но длина знакосочетания $\boldsymbol{I}$ не есть длина знакосочетания $\boldsymbol{O}$, а длина знакосочетания $\tau_w(\boldsymbol{I})$ есть длина знакосочетания $\tau_w(\boldsymbol{O})$. Значит, длина знакосочетания $\boldsymbol{I}$ есть длина знакосочетания $\boldsymbol{O}$.
    
    Пусть p есть длина знакосочетания $\boldsymbol{I}$. Рассмотрим i = 1, 2, ..., p. Возьмем наименьший i, если такой есть, для которого знак под номером i в знакосочетании $\boldsymbol{I}$ не есть знак под номером i в знакосочетании $\boldsymbol{O}$.
    
    Пусть знак под номером i в знакосочетании $\boldsymbol{I}$ есть $\boldsymbol{w}$ или знак под номером i в знакосочетании $\boldsymbol{O}$ есть $\boldsymbol{w}$, тогда, по определению операции $\tau_u(\boldsymbol{C})$, где $\boldsymbol{C}$ - знакосочетание, а $\boldsymbol{u}$ - буква, либо знак под номером i + 1 в знакосочетании $\tau_w(\boldsymbol{I})$ есть знак $\square$, связанный со знаком $\tau$, написанным слева от $\boldsymbol{I}$, а знак под номером i + 1 в знакосочетании $\tau_w(\boldsymbol{O})$ не есть знак $\square$, связанный со знаком $\tau$, написанным слева от $\boldsymbol{O}$, либо знак под номером i + 1 в знакосочетании $\tau_w(\boldsymbol{O})$ есть знак $\square$, связанный со знаком $\tau$, написанным слева от $\boldsymbol{O}$, а знак под номером i + 1 в знакосочетании $\tau_w(\boldsymbol{I})$ не есть знак $\square$, связанный со знаком $\tau$, написанным слева от $\boldsymbol{I}$. Тогда $\tau_w(\boldsymbol{I})$ не есть $\tau_w(\boldsymbol{O})$, но $\tau_w(\boldsymbol{I})$ есть $\tau_w(\boldsymbol{O})$. Значит, ни знак под номером i в знакосочетании $\boldsymbol{I}$ не есть $\boldsymbol{w}$, ни знак под номером i в знакосочетании $\boldsymbol{O}$ не есть $\boldsymbol{w}$.
    
    По определению операции $\tau_u(\boldsymbol{C})$, где $\boldsymbol{C}$ - знакосочетание, а $\boldsymbol{u}$ - буква, знакосочетание $\tau_u(\boldsymbol{C})$ не изменяет никакие знаки знакосочетания $\boldsymbol{C}$ отличные от $\boldsymbol{u}$. Значит, знак под номером i + 1 в знакосочетании $\tau_w(\boldsymbol{O})$ не есть знак под номером i + 1 в знакосочетании $\tau_w(\boldsymbol{I})$. Значит $\tau_w(\boldsymbol{I})$ не есть $\tau_w(\boldsymbol{O})$, но $\tau_w(\boldsymbol{I})$ есть $\tau_w(\boldsymbol{O})$.

    Пусть p есть длина знакосочетания $\boldsymbol{I}$. Рассмотрим i = 1, 2, ..., p. Возьмем наименьший i, если такой есть, для которого связь знака под номером i со знаком под номером j в знакосочетании $\boldsymbol{I}$ не есть связь знака под номером i со знаком под номером j в знакосочетании $\boldsymbol{O}$.

    Так как $\tau_w(\boldsymbol{I})$ есть $\tau_w(\boldsymbol{O})$, связи $\tau_w(\boldsymbol{I})$ есть связи $\tau_w(\boldsymbol{O})$. Значит связь, которая есть в $\boldsymbol{O}$ (соответственно $\boldsymbol{I}$), но нет в $\boldsymbol{I}$ (соответственно $\boldsymbol{O}$) должна появится при операции перехода от $\boldsymbol{I}$ (соответственно $\boldsymbol{O}$) к $\tau_w(\boldsymbol{I})$ (соотвественно $\tau_w(\boldsymbol{O})$), но по определению операции $\tau_u(\boldsymbol{C})$, где $\boldsymbol{C}$ - знакосочетание, а $\boldsymbol{u}$ - буква, в знакосочетании $\tau_u(\boldsymbol{C})$ связи, отличные от связей $\boldsymbol{C}$, суть связи знака $\tau$, написанного слева от $\boldsymbol{C}$, с какими-либо знаками в $\boldsymbol{C}$, значит, связь, которая есть в $\boldsymbol{O}$ (соответственно $\boldsymbol{I}$), но нет в $\boldsymbol{I}$ (соответственно $\boldsymbol{O}$) не может появится при операции перехода от $\boldsymbol{I}$ (соответственно $\boldsymbol{O}$) к $\tau_w(\boldsymbol{I})$ (соотвественно $\tau_w(\boldsymbol{O})$), так как этого знака $\tau$ нету в $\boldsymbol{O}$ (соотвественно $\boldsymbol{I}$), а при $\tau_u(\boldsymbol{C})$ появляются лишь связи с этим знаком $\tau$.

    Длина $\boldsymbol{I}$ есть длина $\boldsymbol{O}$, последовательность знаков $\boldsymbol{I}$ есть последовательность знаков $\boldsymbol{O}$, связи $\boldsymbol{I}$ суть связи $\boldsymbol{O}$.
    
    Знакосочетание $\boldsymbol{I}$ есть знакосочетание $\boldsymbol{O}$.

2. Пусть $\boldsymbol{B}$ содержит $\boldsymbol{x}$, значит, $\tau_y(\boldsymbol{A_m})\boldsymbol{B}$ содержит $\boldsymbol{x}$. Но $\tau_x(\boldsymbol{A}_j)$, по определению операции $\tau_u(\boldsymbol{C})$, где $\boldsymbol{C}$ - знакосочетание, а $\boldsymbol{u}$ - буква, не содержит $\boldsymbol{x}$. Значит, $\boldsymbol{B}$ не содержит $\boldsymbol{x}$.

    Пусть $\boldsymbol{D}$ - знакосочетание теории $\mathcal{T}$. Покажем, что $\tau_x(\boldsymbol{D})\boldsymbol{B}$ есть $\tau_x(\boldsymbol{D}\boldsymbol{B})$.

    Сравним операцию перехода от $\boldsymbol{D}$ и $\boldsymbol{B}$ к $\tau_x(\boldsymbol{D})\boldsymbol{B}$ с операцией перехода от $\boldsymbol{D}$ и $\boldsymbol{B}$ к $\tau_x(\boldsymbol{D}\boldsymbol{B})$. При обеих операциях мы пишем слева направо знакосочетание $\tau\boldsymbol{D}\boldsymbol{B}$, соединяем связью каждый экземпляр буквы $\boldsymbol{x}$ в $\boldsymbol{D}$ со знаком $\tau$, написанным слева от $\boldsymbol{D}$, и заменяем букву $\boldsymbol{x}$, каждый ее экземпляр в $\boldsymbol{D}$ символом $\square$. При обеих операциях не изменяется ни один знак, содержащийся в $\boldsymbol{B}$: это очевидно для первой операции, а для второй это вытекает из того, что $\boldsymbol{x}$ не содержится в $\boldsymbol{B}$. При обеих операциях ни один другой знак не изменяется.

3. Пусть $\boldsymbol{y}$ есть $\boldsymbol{x}$, значит, $\tau_y(\boldsymbol{A}_m)$ есть $\tau_x(\boldsymbol{A}_m)$. То есть $\tau_x(\boldsymbol{A}_j)$ есть $\tau_x(\boldsymbol{A}_m)\boldsymbol{B}$.

    Пусть $\boldsymbol{y}$ не есть ${\boldsymbol{x}}$. 

    Предположим, что $\boldsymbol{A}_m$ содержит $\boldsymbol{x}$. Тогда, знакосочетание $\tau_y({\boldsymbol{A}_m})$, по определению, содержит $\boldsymbol{x}$, значит, $\tau_y({\boldsymbol{A}_m})\boldsymbol{B}$ содержит $\boldsymbol{x}$. Но знакосочетание $\tau_x(\boldsymbol{A}_j)$, по определению, не содержит $\boldsymbol{x}$. Значит, $\boldsymbol{A}_m$ не содержит x.
    
    Согласно CS3, $\tau_y(\boldsymbol{A}_m)$ есть $\tau_x((\boldsymbol{x}|{\boldsymbol{y}})\boldsymbol{A}_m)$. Пусть $\boldsymbol{V}$ есть $(\boldsymbol{x}|\boldsymbol{y})\boldsymbol{A}_m$. Согласно CF7, $\boldsymbol{V}$ есть соотношение теории $\mathcal{T}$, значит, $\tau_y(\boldsymbol{A}_m)$ есть $\tau_x(\boldsymbol{V})$. То есть $\tau_x(\boldsymbol{A}_j)$ есть $\tau_x(\boldsymbol{V})\boldsymbol{B}$.

4. Согласно 3, $\tau_x(\boldsymbol{A}_j)$ есть либо $\tau_x(\boldsymbol{A}_m)\boldsymbol{B}$, либо $\tau_x(\boldsymbol{V})\boldsymbol{B}$, где $\boldsymbol{V}$ есть соотношение теории $\mathcal{T}$. Согласно 2, $\tau_x(\boldsymbol{A}_m)\boldsymbol{B}$, $\tau_x(\boldsymbol{V})\boldsymbol{B}$ суть соответственно $\tau_x(\boldsymbol{A}_m\boldsymbol{B})$, $\tau_x(\boldsymbol{V}\boldsymbol{B})$. 

5. Так как $\boldsymbol{A}\boldsymbol{B}$ есть $\tau_x(\boldsymbol{A}_j)$, а согласно 4, $\tau_x(\boldsymbol{A}_j)$ есть либо $\tau_x(\boldsymbol{A}_m\boldsymbol{B})$, либо $\tau_x(\boldsymbol{V}\boldsymbol{B})$, где $\boldsymbol{V}$ есть соотношение теории $\mathcal{T}$, значит, согласно 1, $\boldsymbol{A}_j$ есть либо $\boldsymbol{A}_m\boldsymbol{B}$, либо $\boldsymbol{V}\boldsymbol{B}$. Длина каждого из занакосочетаний $\boldsymbol{A}_m\boldsymbol{B}$ и $\boldsymbol{V}\boldsymbol{B}$ меньше длины $\boldsymbol{A}_n$, но мы уже установили, что термы и соотношения длины меньше чем n, не есть $\boldsymbol{HY}$, где $\boldsymbol{H}$ - терм или соотношение теории $\mathcal{T}$, а $\boldsymbol{Y}$ - знакосочетание теории $\mathcal{T}$.

д) $\boldsymbol{A}_n$ есть $\boldsymbol{s}\boldsymbol{J}_1\boldsymbol{J}_2...\boldsymbol{J}_p$, где $\boldsymbol{s}$ - специальный знак веса p из $\mathcal{T}$, а $\boldsymbol{J}_1$, $\boldsymbol{J}_2$, ..., $\boldsymbol{J}_p$ - p термов теории $\mathcal{T}$. Так как $\boldsymbol{AB}$ есть $\boldsymbol{A}_n$, значит, $\boldsymbol{A}$ начинается со знака $\boldsymbol{s}$. Согласно 4.4, $\boldsymbol{A}$ есть $\boldsymbol{s}\boldsymbol{M}_1\boldsymbol{M}_2...\boldsymbol{M}_p$, где $\boldsymbol{M}_1$, $\boldsymbol{M}_2$, ..., $\boldsymbol{M}_p$ - p термов теории $\mathcal{T}$. Значит, $\boldsymbol{s}\boldsymbol{J}_1\boldsymbol{J}_2...\boldsymbol{J}_p$ есть $\boldsymbol{s}\boldsymbol{M}_1\boldsymbol{M}_2...\boldsymbol{M}_p\boldsymbol{B}$.

Если p есть 0, значит, $\boldsymbol{A}_n$ есть $\boldsymbol{s}$, то есть имеет длину 1, но $\boldsymbol{AB}$ имеет длину, как минимум, 2. Значит, p больше 0.

Рассмотрим i = 1, 2, ..., p-1. Если существует такое i, выберем наименьшее из них, для которого длина знакосочетания $\boldsymbol{J}_i$ не есть длина знакосочетания $\boldsymbol{M}_i$. Пусть $\boldsymbol{Y}$ - знакосочетание теории $\mathcal{T}$, тогда, согласно 4.3, либо $\boldsymbol{J}_i$ есть $\boldsymbol{M}_i\boldsymbol{Y}$, либо $\boldsymbol{M}_i$ есть $\boldsymbol{J}_i\boldsymbol{Y}$. Длина каждого из знакосочетаний $\boldsymbol{J}_i\boldsymbol{Y}$ и $\boldsymbol{M}_i\boldsymbol{Y}$ меньше длины $\boldsymbol{A}_n$, но мы уже установили, что термы и соотношения длины меньше чем n, не есть $\boldsymbol{HY}$, где $\boldsymbol{H}$ - терм или соотношение теории $\mathcal{T}$, а $\boldsymbol{Y}$ - знакосочетание теории $\mathcal{T}$.

Если такого i не существует, значит, $\boldsymbol{J}_p$ есть $\boldsymbol{M}_p\boldsymbol{B}$. Длина знакосочетания $\boldsymbol{M}_p\boldsymbol{B}$ меньше длины $\boldsymbol{A}_n$, но мы уже установили, что термы и соотношения длины меньше чем n, не есть $\boldsymbol{HY}$, где $\boldsymbol{H}$ - терм или соотношение теории $\mathcal{T}$, а $\boldsymbol{Y}$ - знакосочетание теории $\mathcal{T}$.

Пусть $\boldsymbol{A}$ - соотношение или терм теории $\mathcal{T}$, $\boldsymbol{A}_n$ - соотношение или терм этой теории длины n, а $\boldsymbol{B}$ - знакосочетание этой теории. Знакосочетание $\boldsymbol{AB}$ не есть $\boldsymbol{A}_n$.

Пусть знакосочетание $\boldsymbol{AB}$ есть терм или соотношение теории $\mathcal{T}$ длины n, тогда $\boldsymbol{AB}$ есть $\boldsymbol{A}_n$, но $\boldsymbol{AB}$ не есть $\boldsymbol{A}_n$, значит, знакосочетание $\boldsymbol{AB}$ не есть ни терм, ни соотношение теории $\mathcal{T}$.